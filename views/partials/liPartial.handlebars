<section class="gallery-block grid-gallery">
    <div class="container">
        <div class="row">
            {{#each recipes}}
                {{> liItemPartial}}
            {{/each}}
        </div>
    </div>
</section>

